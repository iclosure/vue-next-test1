import { createStore } from "vuex";

interface State {
  counter: number;
}

export default createStore({
  state: {
    counter: 0
  },
  mutations: {},
  actions: {},
  modules: {},
  getters: {}
});
